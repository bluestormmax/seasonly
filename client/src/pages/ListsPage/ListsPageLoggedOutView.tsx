import { Typography } from "@mui/material";

const ListsPageLoggedOutView = () => {
  return (
    <Typography variant="body1" align="center">
      Please log in to view your saved lists.
    </Typography>
  );
};

export { ListsPageLoggedOutView };
